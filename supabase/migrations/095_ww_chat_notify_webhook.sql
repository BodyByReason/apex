-- Webhook trigger not needed: ww-chat-notify is called directly from the app
-- client after each successful chat message insert (see WalkWaterCommunityScreen.tsx).
-- pg_net / supabase_functions are not enabled on this project.
SELECT 1;
